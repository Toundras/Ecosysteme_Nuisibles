namespace TP_Note
{
    public struct Position
    {
        public uint x;
        public uint y;

        // Coordonnées des Nuisibles dans l'Ecosystème
        public Position(uint x, uint y)
        {
            this.x = x;
            this.y = y;
        }
    }
}